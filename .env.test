# Vite 环境变量，只有以 VITE_ 为前缀的变量才会暴露到全局
VITE_NODE_ENV = 'test'

# 站点 title
VITE_GLOB_APP_TITLE = vue-vite-web-admin

# 请求的根路径
VITE_GLOB_API_URL = 'http://test.api.woa.com'

# 是否开启 mock
VITE_USE_MOCK = false

# public path
VITE_PUBLIC_PATH = ./

# 请求代理配置
# VITE_PROXY=[["/api","http://test.api.woa.com"]]

# 删除打印日志
VITE_DROP_CONSOLE = false